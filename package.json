{
  "name": "neo-companion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.32.0",
  "scripts": {
    "dev": "node scripts/dev.mjs web",
    "dev:tauri": "node scripts/dev.mjs tauri",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "6.0.3",
    "vitest": "4.1.7"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  }
}
